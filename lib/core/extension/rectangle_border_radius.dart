import 'package:flutter/material.dart';

extension RectangleBorderRadius on BuildContext {
  Radius get leftRadius => const Radius.circular(150);
  Radius get rightRadius => const Radius.circular(15);
}
